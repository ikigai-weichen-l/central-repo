apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: live-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: live
  source:
    repoURL: https://github.com/ikigai-weichen-l/central-repo.git
    targetRevision: main
    path: apps/live/base
  destination:
    server: https://kubernetes.default.svc
    namespace: live # deployment target
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
