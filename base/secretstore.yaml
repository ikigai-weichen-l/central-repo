apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: fake-store
spec:
  provider:
    fake:
      data:
        - key: "DATABASE_URL"
          value: "this-is-a-test-value"
        - key: "API_KEY"
          value: "this-is-a-test-value-too"
